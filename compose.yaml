services:
  telegram-bot:
    build: .
    container_name: telegram-ai-bot
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "8080:8080"

    command: ["python", "-m", "src.telegram_bot.main", "--mode", "polling"]

    volumes:
      - ./src:/app/src
